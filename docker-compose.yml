version: '3.8'

services:
  myapp:
    build: ./myapp
    container_name: myapp_container
    expose:
      - "80"
    restart: always

  nginx:
    build: ./nginx
    container_name: nginx_proxy
    ports:
      - "80:80"
    depends_on:
      - myapp
    restart: always